'use strict'

const db = require('../db')

const tableName = '...'

async function insert(params) {}
async function read(params) {}
async function insertOrReplace(params) {}

module.exports = {
  tableName,
  insert,
  insertOrReplace,
  read
}
