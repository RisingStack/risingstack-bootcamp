'use strict'

const { expect } = require('chai')
const db = require('../db')
const Repository = require('./repository')

describe('Repository', () => {
  beforeEach(async () => {})

  afterEach(async () => {})

  describe('.insert', () => {
    // ...
  })

  describe('.read', () => {
    // ...
  })
})
